query GetCarServices($carId: Int, $limit: Int) {
    getCarServices (params: { carId: $carId, limit: $limit }) {
        services {
            id,
            name,
            createdAt
        }
    }
}